ssh install ubuntu 22.04

# sudo apt-get install openssh.server
# systemctl status sshd
# sudo apt-get install openssh.client
# sudo ufw allow ssh
# sudo ufw allow 22/tcp
# ufw allow 80

## sudo nano /etc/ssh/sshd_config
